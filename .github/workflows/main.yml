name: KyrooS Build System

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Build KyrooS APK
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: 8.4

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Generate Gradle Wrapper
      run: |
        gradle wrapper --gradle-version 8.4
        chmod +x gradlew

    - name: Build Debug APK
      run: ./gradlew assembleDebug

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: KyrooS-Debug
        path: app/build/outputs/apk/debug/app-debug.apk

    - name: Build Release APK (Unsigned)
      run: ./gradlew assembleRelease

    - name: Rename Unsigned APK
      run: |
        mv app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/KyrooS-Release-unsigned.apk

    - name: Upload Unsigned Release APK
      uses: actions/upload-artifact@v4
      with:
        name: KyrooS-Release-Unsigned
        path: app/build/outputs/apk/release/KyrooS-Release-unsigned.apk
